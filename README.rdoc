= Delayed Job Admin

Simple admin interface for Delayed::Job (http://rubygems.org/gems/delayed_job)

= Usage

- Install the gem:

    # Gemfile
    gem "delayed_job_admin"

    bundle install

- Setup authentication:

    # todo

- View the admin interface

    open http://localhost:3000/delayed_job_admin

= Todo

x index
- show
- destroy
- restart
- restart all
- require admin login, docs
